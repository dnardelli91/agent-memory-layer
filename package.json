{
  "name": "agent-memory-layer",
  "version": "0.1.0",
  "description": "Local-first project memory for AI agents. Git-aware, MCP-native, zero-config.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agent-memory": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "commander": "^12.1.0",
    "simple-git": "^3.27.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
